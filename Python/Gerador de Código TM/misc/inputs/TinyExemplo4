PROGRAM
VAR NUM : INTEGER;
VAR MAX : INTEGER;
VAR MIN : INTEGER;
BEGIN
    READ NUM;
    MAX := NUM;
    MIN := NUM;
    WHILE NUM <> 999 DO
    BEGIN
        IF NUM > MAX THEN MAX:=NUM END;
        IF NUM < MIN THEN MIN:=NUM END;
        READ NUM
    END;
    WRITE MAX;
    WRITE MIN
END